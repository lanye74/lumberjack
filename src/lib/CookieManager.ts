import type {Cookies} from "@sveltejs/kit";
import type {SupabaseClient} from "@supabase/supabase-js";



export default class CookieManager {
	constructor(cookies: Cookies, supabase: SupabaseClient) {

	}
}
